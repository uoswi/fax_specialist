---
const problems = [
  {
    title: "Vendor Runaround",
    description: "Tired of being bounced between sales, support, and account managers who don't know your setup?",
    iconColor: "text-red-500",
    bgColor: "bg-red-50",
    borderColor: "border-red-100"
  },
  {
    title: "Offshore Support",
    description: "Fed up with support teams that can't understand your compliance requirements or technical needs?",
    iconColor: "text-orange-500",
    bgColor: "bg-orange-50",
    borderColor: "border-orange-100"
  },
  {
    title: "No Accountability",
    description: "Struggling to find someone who will actually own the problem when faxes fail at 2 AM?",
    iconColor: "text-amber-500",
    bgColor: "bg-amber-50",
    borderColor: "border-amber-100"
  },
];

const solutions = [
  {
    title: "One Expert",
    description: "Direct access to someone who's built, scaled, and sold fax infrastructure. No layers, no runaround."
  },
  {
    title: "Deep Expertise",
    description: "HIPAA, SOC2, enterprise compliance — I've done it for banks, hospitals, and government agencies."
  },
  {
    title: "Results, Not Promises",
    description: "I've helped enterprises cut fax costs by 60% while improving reliability. Let's do it for you."
  },
];
---

<section class="py-24 lg:py-32 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-1.5 rounded-full bg-gray-100 text-gray-600 text-sm font-medium mb-4">
        The Problem
      </span>
      <h2 class="text-3xl lg:text-4xl font-display font-bold text-gray-900 mb-4">
        Sound Familiar?
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Enterprise fax shouldn't be this hard. Here's what I hear from IT leaders every week.
      </p>
    </div>

    <!-- Problems Grid -->
    <div class="grid md:grid-cols-3 gap-6 mb-20">
      {problems.map((problem, index) => (
        <div class={`relative bg-white p-8 rounded-2xl border ${problem.borderColor} shadow-sm hover:shadow-lg transition-all duration-300 group`}>
          <!-- Warning icon -->
          <div class={`w-14 h-14 rounded-xl ${problem.bgColor} ${problem.iconColor} flex items-center justify-center mb-6 group-hover:scale-110 transition-transform`}>
            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
          </div>
          <h3 class="text-xl font-display font-semibold text-gray-900 mb-3">{problem.title}</h3>
          <p class="text-gray-600 leading-relaxed">{problem.description}</p>
        </div>
      ))}
    </div>

    <!-- Transition Arrow -->
    <div class="flex justify-center mb-20">
      <div class="flex flex-col items-center">
        <div class="w-px h-16 bg-gradient-to-b from-gray-200 to-indigo"></div>
        <div class="w-12 h-12 rounded-full bg-indigo flex items-center justify-center shadow-lg shadow-indigo/30">
          <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
          </svg>
        </div>
      </div>
    </div>

    <!-- Solution Section -->
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-1.5 rounded-full bg-indigo-50 text-indigo font-medium text-sm mb-4">
        The Solution
      </span>
      <h2 class="text-3xl lg:text-4xl font-display font-bold text-gray-900 mb-4">
        Here's How I'm Different
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        25+ years of enterprise fax experience, distilled into one direct relationship.
      </p>
    </div>

    <!-- Solutions Grid -->
    <div class="grid md:grid-cols-3 gap-6">
      {solutions.map((solution, index) => (
        <div class="relative bg-gradient-to-br from-indigo-50 to-white p-8 rounded-2xl border border-indigo-100 shadow-sm hover:shadow-xl hover:shadow-indigo/10 transition-all duration-300 group">
          <!-- Number badge -->
          <div class="absolute -top-3 -left-3 w-10 h-10 rounded-full bg-indigo text-white flex items-center justify-center font-bold shadow-lg shadow-indigo/30">
            {index + 1}
          </div>

          <!-- Check icon -->
          <div class="w-14 h-14 rounded-xl bg-indigo/10 text-indigo flex items-center justify-center mb-6 group-hover:bg-indigo group-hover:text-white transition-all">
            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
            </svg>
          </div>

          <h3 class="text-xl font-display font-semibold text-gray-900 mb-3">{solution.title}</h3>
          <p class="text-gray-600 leading-relaxed">{solution.description}</p>
        </div>
      ))}
    </div>

    <!-- CTA -->
    <div class="mt-16 text-center">
      <a
        href="/contact"
        class="inline-flex items-center gap-2 px-8 py-4 rounded-xl font-semibold text-lg transition-all duration-200 shadow-lg hover:shadow-xl"
        style="background-color: #6366F1; color: #ffffff;"
      >
        Let's Solve Your Fax Challenges
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
    </div>
  </div>
</section>
